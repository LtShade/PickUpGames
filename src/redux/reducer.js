import { STATICRESULTS, CATEGORIES } from "../js/searchResults";

export const initialState = {
  categories: CATEGORIES,
  staticresults: STATICRESULTS,
};

export const Reducer = (state = initialState, action) => {
  return state;
};
